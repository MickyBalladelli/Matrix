# Matrix <version> — <date>

Use this file as `docs/releases/<version>.md`. Replace the placeholders and
keep release notes concrete. The same bullet list is added to `CHANGELOG.md` by
`npm run release:prepare`.

- Describe the most important user-facing change.
- Mention migration or compatibility impact.
- Mention the verified package and example-app state.
